import { ClickOutSideBarDirective } from './click-out-side-bar.directive';

describe('ClickOutSideBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutSideBarDirective();
    expect(directive).toBeTruthy();
  });
});
