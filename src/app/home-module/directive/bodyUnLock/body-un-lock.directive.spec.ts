import { BodyUnLockDirective } from './body-un-lock.directive';

describe('BodyUnLockDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyUnLockDirective();
    expect(directive).toBeTruthy();
  });
});
